# Documentation Index

This directory hosts design references, ADRs, and operational runbooks that complement the top-level plan in `DevOps_LLM_Agent_Plan.md`.

- Place runbooks under `docs/runbooks/` grouped by service or workflow.
- Document new architecture decisions via ADRs (`docs/adr/`) once the subsystem is implemented.
- Update this index when new doc sections ship.
